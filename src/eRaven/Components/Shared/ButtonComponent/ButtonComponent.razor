<button class="btn @Class"
        type="button"
        style="@Style"
        disabled="@IsDisabled"
        @onclick="ClickButton">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else if (!string.IsNullOrWhiteSpace(Label))
    {

        @Label
    }
</button>