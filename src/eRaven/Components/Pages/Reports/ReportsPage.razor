@* @page "/reports"

<PageTitle>Звіти</PageTitle>

<div class="container-fluid py-3" style="min-height:60vh;">
    <h5 class="mb-3">Звіти</h5>

    <!-- Група посилань: можна додавати нові пункти без зміни логіки -->
    <div class="list-group shadow-sm">

        <!-- Особовий склад на дату (сторінка-параметризатор або готова сторінка, коли буде) -->
        <NavLink class="list-group-item list-group-item-action d-flex align-items-center"
                 href="/reports/staff-on-date">
            <i class="bi bi-people me-2"></i>
            <div>
                <div class="fw-semibold">Особовий склад на дату</div>
                <div class="small text-muted">Станом на обрану дату; структура та кількісні показники.</div>
            </div>
        </NavLink>

        <!-- Планування на дату (сторінка-параметризатор або готова сторінка, коли буде) -->
        <NavLink class="list-group-item list-group-item-action d-flex align-items-center"
                 href="/reports/planning-on-date">
            <i class="bi bi-airplane me-2"></i>
            <div>
                <div class="fw-semibold">Екіпажи на дату</div>
                <div class="small text-muted">Станом на обрану дату; Локація, група та типи.</div>
            </div>
        </NavLink>

        <!-- Звіт за місяць по статусах (30/100/тощо) -->
        <NavLink class="list-group-item list-group-item-action d-flex align-items-center"
                 href="/reports/status-month">
            <i class="bi bi-bar-chart-line me-2"></i>
            <div>
                <div class="fw-semibold">Місячний звіт по статусах</div>
                <div class="small text-muted">Агрегація за кодами (30, 100, В, ВДР, Л_Х…)</div>
            </div>
        </NavLink>

        <!-- Місце для майбутніх звітів -->
      @*   <div class="list-group-item d-flex align-items-center bg-light">
            <i class="bi bi-plus-circle me-2 text-muted"></i>
            <div>
                <div class="fw-semibold text-muted">Додати новий звіт…</div>
                <div class="small text-muted">Тут легко додати ще один пункт меню, коли з’явиться нова сторінка.</div>
            </div>
        </div> *@
    </div>
</div>
 *@